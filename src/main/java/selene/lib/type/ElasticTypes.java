package selene.lib.type;

public enum ElasticTypes {
        TEXT,
        KEYWORD,
        BYTE,
        SHORT,
        INTEGER,
        LONG,
        FLOAT,
        DOUBLE,
        BOOLEAN,
        OBJECT,
        NESTED,
        IP,
        GEO_POINT,
        BINARY,
        DATE,
    WILDCARD,
    TEXT_WITH_KEYWORD,
        UNKNOWN;
}
